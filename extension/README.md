# 🧩 포트폴리오 확장 프로그램

두 가지를 합니다.

1. **시세를 내 브라우저가 직접 가져옵니다** — 종목이 빈 칸으로 나오는 문제가 없어집니다.
2. **툴바 아이콘으로 앱을 바로 엽니다** — 주소를 칠 필요가 없습니다.
   (이미 열려 있으면 그 탭으로 이동합니다)

크롬·엣지에서 됩니다. (사파리·휴대폰은 안 됩니다)

---

# 설치 방법

## 1단계 — 파일 받기

아래 파일을 내려받고 **압축을 풉니다.**

📦 [portfolio-proxy-extension.zip 받기](https://github.com/statclaude/portfolio-web/releases/latest)

압축을 풀면 `manifest.json` 등이 들어 있는 폴더가 생깁니다. **이 폴더를 기억해 두세요.**

> 압축을 꼭 풀어야 합니다. zip 파일 그대로는 등록되지 않습니다.

## 2단계 — 확장 관리 화면 열기

크롬 주소창에 아래를 입력하고 엔터:

```
chrome://extensions
```

엣지는 `edge://extensions` 입니다.

## 3단계 — 개발자 모드 켜기

화면 **오른쪽 위**의 `개발자 모드` 스위치를 켭니다.
켜면 위쪽에 버튼 몇 개가 새로 나타납니다.

## 4단계 — 폴더 등록

방금 나타난 **`압축해제된 확장 프로그램을 로드합니다`** 버튼을 누르고,
1단계에서 압축을 푼 **폴더를 선택**합니다.

> 폴더 안의 파일이 아니라 **폴더 자체**를 선택해야 합니다.

## 5단계 — 툴바에 고정

주소창 오른쪽의 **퍼즐 조각 아이콘(🧩)** 을 누르고,
**포트폴리오 시세 프록시** 옆의 **압정(📌)** 을 눌러 고정합니다.

**끝났습니다.** 툴바에 생긴 아이콘을 누르면 앱이 새 탭으로 열립니다.

---

# 잘 되고 있는지 확인

앱에서 **⚙️ 설정 → 내 전용 프록시** 를 여세요.

| 표시 | 뜻 |
|---|---|
| 🟢 **🧩 크롬 확장 사용 중** | 정상입니다 |
| 회색 안내만 보임 | 아래 "문제 해결" 참고 |

---

# 업데이트

개발자 모드로 설치한 확장은 **자동으로 업데이트되지 않습니다.**
그래서 앱이 대신 알려줍니다.

새 버전이 나오면 **⚙️ 설정 → 내 전용 프록시** 에 이렇게 표시됩니다.

> ⚠️ **새 확장 버전 v1.1.0** 이 나왔습니다 (현재 v1.0.0).
> 개발자 모드 확장은 자동 업데이트가 없어 **새 zip 을 받아 다시 등록**해야 합니다.

**업데이트 방법**

1. 새 zip 을 받아 압축을 풉니다 (기존 폴더에 덮어써도 됩니다)
2. `chrome://extensions` 에서 이 확장의 **새로고침(⟳)** 버튼을 누릅니다
   - 폴더 위치가 바뀌었으면 기존 것을 **삭제**하고 설치 4단계부터 다시 하세요
3. 앱 화면을 새로고침합니다

경고가 사라지면 완료입니다.

---

# 자주 묻는 것

**꼭 툴바 아이콘으로 열어야 하나요?**
아니요. 평소처럼 북마크나 주소로 들어가셔도 확장이 똑같이 동작합니다.
아이콘은 주소 치기 귀찮을 때 쓰는 편의 기능입니다.

**기존 데이터가 사라지나요?**
아니요. 웹에서 쓰던 주소를 그대로 열기 때문에 보유·거래 기록, 설정,
구글 로그인 상태가 전부 그대로입니다. 확장을 지워도 앱은 평소대로 동작합니다.

**크롬을 켤 때마다 "개발자 모드 확장을 사용 중지하세요" 라고 떠요**
개발자 모드로 설치하면 크롬이 매번 물어봅니다. **무시하셔도 됩니다.**
(웹스토어에 등록하면 없어지지만 아직 등록 전입니다)

**프록시 설정은 어떻게 하나요?**
그대로 두시면 됩니다. 확장이 처리하지 못하는 일부(차트·과거 시세·미국 지수)는
기존 프록시를 계속 사용합니다.

---

# 문제 해결

**설정에 `크롬 확장 사용 중` 이 안 뜰 때**

1. `chrome://extensions` 에서 확장이 **사용 중(파란 스위치)** 인지 확인
2. 앱 화면에서 새로고침(`Ctrl+R` / `Cmd+R`)
3. 그래도 안 되면 확장을 지우고 4단계부터 다시

**시세가 안 나올 때**

`chrome://extensions` 에서 이 확장의 **`서비스 워커`** 링크를 누르면 개발자 도구가 열립니다.
빨간 오류 메시지가 있으면 그 내용을 알려주세요.

---

---

# 기술 메모

## 왜 필요한가

토스는 **egress IP 풀 단위**로 요청을 막습니다. Cloudflare·Netlify 같은 클라우드
프록시는 나가는 IP를 전 세계 고객이 공유해서, 내 호출량이 0이어도 그 풀이 붐비면
같이 막힙니다. 실측에서 개인 Cloudflare 워커 2개가 **동시에** `400 + 빈 본문`을 받았고,
지수(`c-chart`)는 통과하는데 종목 시세(`stock-prices`)만 막혀 **보유 그룹만 통째로 비었습니다.**

확장은 요청을 **내 브라우저에서** 보냅니다. 가정용 IP라 그 차단에 걸리지 않고,
호출 한도도 없습니다. 기존 [내 PC 로컬 프록시](../workers/local-proxy/README.md)를
대체합니다 — Node 설치도, 터미널도, 서버를 켜두는 것도 필요 없습니다.

## 왜 팝업·사이드 패널이 아니라 탭인가

둘 다 앱을 iframe 으로 감싸야 하는데 크기 제약이 따라온다. 팝업은 최대 800×600 이
하드 상한이고, 사이드 패널은 세로는 창 높이만큼 쓰지만 폭이 좁아 축소(zoom)가 필요했다.
탭으로 열면 브라우저 크기 그대로라 제약이 없고, 데스크톱 표 화면이 정상으로 나온다.
콘텐트 스크립트는 URL 기준으로 주입되므로 프록시 동작은 어느 쪽이든 동일하다.

## 동작

```
앱 (탭)
   │ window.postMessage
콘텐트 스크립트 (content.js)
   │ chrome.runtime.sendMessage
서비스워커 (background.js)
   │ fetch  ← 여기가 내 IP
토스 · 네이버 · yasun · investing · TradingView · Yahoo
```

- **호스트 화이트리스트** — 위 목록만. 그 외 도메인은 거부합니다.
- **헤더 재작성** — 토스는 `Origin`이 `tossinvest.com`이 아니면 `403`입니다
  (실측: Origin 없음 `200` / `chrome-extension://` `403` / `tossinvest.com` `200`).
  `Origin`·`Referer`는 `fetch()`로 못 바꾸는 금지 헤더라 `declarativeNetRequest`로 갈아끼웁니다.
  이 규칙에는 **`tabIds: [-1]`** 조건이 붙어 **확장이 보낸 요청에만** 적용됩니다 —
  사용자가 브라우저로 토스·네이버를 직접 볼 때는 아무 영향이 없습니다.
- **바이트 그대로 전달** — 네이버 자금동향은 EUC-KR이라 텍스트로 옮기면 깨집니다.
  응답을 base64로 넘기고 디코딩은 앱이 합니다.
- **권한** — `declarativeNetRequest` 하나뿐입니다. 이미 열린 탭을 찾는 데 필요한
  앱 주소(`statclaude.github.io`)는 호스트 권한으로만 받습니다. `tabs` 권한을 쓰면
  "방문 기록 읽기" 경고가 뜨는데, 호스트 하나만 받으면 그게 없습니다.

## Yahoo — 확장이라서 되는 부분

Yahoo는 **IP가 아니라 "브라우저가 아닌 클라이언트"를 막습니다.** 실측:

| 클라이언트 | 결과 |
|---|---|
| curl (같은 회선, 브라우저 헤더·HTTP/2 맞춤) | `429 Edge: Too Many Requests` |
| **확장 서비스워커 fetch** | **`200`** |

TLS/HTTP2 지문으로 걸러내는 것이라, 헤더를 아무리 흉내내도 curl로는 못 넘습니다.
브라우저 네트워크 스택으로 나가는 확장은 그대로 통과합니다.

`v7/quote`·`quoteSummary`는 crumb 인증이 필요해 crumb 없이는 `401`입니다.
확장이 직접 발급합니다 — `fc.yahoo.com`으로 세션 쿠키를 받고 `getcrumb`으로
문자열을 얻어 30분 캐시합니다. 워커와 달리 쿠키를 손으로 나를 필요가 없습니다
(`credentials: "include"` 면 브라우저 쿠키 저장소가 알아서 유지합니다).

> 로컬 Node 프록시(`workers/local-proxy`)는 undici를 쓰므로 curl과 같은 처지라
> Yahoo를 처리하지 못합니다. 앱이 그쪽으로는 Yahoo를 안 보냅니다.

## 아이콘

`extension/icons/icon{16,32,48,128}.png` — **앱 파비콘(`public/favicon.svg`)에서 뽑는다.**
수작업으로 만들지 않는다. 앱 로고를 바꾸면 아래를 다시 돌리면 확장도 따라간다.

```bash
python3 scripts/gen-extension-icons.py
```

SVG 래스터화는 macOS 의 `qlmanage`(QuickLook)를 쓴다 — 별도 설치가 필요 없는 대신
macOS 전용이다. 다른 환경에서는 `rsvg-convert` 등으로 512px PNG 를 만들어
`--from <png>` 으로 넘기면 된다.

> qlmanage 는 SVG 의 투명 배경을 살리지 않고 **불투명 흰색**으로 채운 뒤 로고를
> 캔버스 한쪽에 그린다. 그래서 스크립트가 흰 배경을 투명으로 되돌린 다음
> '흰색이 아닌 픽셀' 기준으로 잘라낸다. 알파 기준으로 자르면 캔버스 전체가
> 잡혀 로고가 구석에 처박힌다.

## 버전 관리

버전이 두 곳에 있고 **반드시 같아야** 합니다.

| 위치 | 역할 |
|---|---|
| `extension/manifest.json` 의 `version` | 확장이 앱에 보고하는 값 |
| `src/lib/extensionProxy.ts` 의 `EXPECTED_EXTENSION_VERSION` | 앱이 "낡았다" 고 판단하는 기준 |

확장은 핸드셰이크(`ready` 메시지)에 자기 버전을 실어 보내고, 앱이 기준값보다 낮으면
설정 화면에 재설치 안내를 띄웁니다. 두 값이 어긋나면 잘못된 안내가 나가므로
`npm run pack:extension` 이 불일치를 검사해 중단시킵니다.

## 배포용 zip 만들기

```bash
# 1) 두 곳의 버전을 함께 올린다
#    extension/manifest.json        "version": "1.1.0"
#    src/lib/extensionProxy.ts      EXPECTED_EXTENSION_VERSION = "1.1.0"
npm run pack:extension          # 버전 불일치면 여기서 멈춘다
# → dist-extension/portfolio-proxy-extension-1.1.0.zip

# 2) 릴리스 — README 의 다운로드 링크가 releases/latest 를 가리킨다
gh release create v1.1.0 dist-extension/*.zip --title "확장 v1.1.0"
```
